version: '3.8'

services:
  web:
    build: .
    ports:
      - "20000:20000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
    working_dir: /app
    restart: unless-stopped
    command: uvicorn app.main:app --host 0.0.0.0 --port 20000 --reload
